<!-- @current_page.id -->
$('#page-canvas').html("<%= escape_javascript render(@page) %>");
$('#left-nav').css('background-color', '<%= @color[:color] %>')
$('#header-page-name').text("<%=@page.title%>")
$('#new-entry-button').html("<%= escape_javascript link_to '+Note', {:controller => "entries", :action => "new", :current_page => @page.id}, :remote => true %>");
